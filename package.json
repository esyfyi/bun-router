{
  "name": "bun-router",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "bun test --coverage",
    "build": "bun build ./index.ts --outdir ./dist  --target bun --minify --sourcemap --format esm"
  }
}